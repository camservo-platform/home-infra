---
all:
  hosts:
    pi-cluster-01.local:
    pi-cluster-02.local:
  children:
    raspberry_pi:
      hosts:
        pi-cluster-01.local: {}
        pi-cluster-02.local: {}
    monitors:
      hosts:
        pi-cluster-01.local: {}
  vars:
    ansible_python_interpreter: /usr/bin/python3
